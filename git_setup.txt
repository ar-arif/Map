Go into the directory containing the project.
Type git init.
Type git add to add all of the relevant files.
You’ll probably want to create a .gitignore file right away, to indicate all of the files you don’t want to track. Use git add .gitignore, too.
Type git commit.



git config --global user.name "AR"
git config --global user.email ar.arif7497.1@gmail.com

git init
git remote add remote_Name https://github.com/username/new_repo
git push -u <remote_Name> master



**{error for refusing push}
git pull --allow-unrelated-histories <remote_Name> master